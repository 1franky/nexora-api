# Perfil de producción (VPS).
#
# Igual que en dev, el datasource se inyecta por variable de entorno (ver
# .env.example / compose.yaml); aquí solo se ajusta lo específico de prod.
spring:
  jpa:
    show-sql: false

logging:
  level:
    root: INFO
    com.nexora.api: INFO

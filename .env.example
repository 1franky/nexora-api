# Variables de entorno para el perfil "prod" (VPS). Copiar a .env y
# completar; .env nunca debe subirse al repositorio.

SPRING_PROFILES_ACTIVE=prod
SERVER_PORT=8080

DB_URL=jdbc:postgresql://localhost:5432/nexora
DB_USERNAME=nexora
DB_PASSWORD=changeme
